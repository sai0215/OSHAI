import React from "react";

const Loading = () => {
  return (
    <div className="w-full h-screen flex justify-center text-center items-center">
      Loading... Fetching from Heroku (for the first time) may take a few
      seconds
    </div>
  );
};

export default Loading;
